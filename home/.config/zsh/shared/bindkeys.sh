# omarchy arch seems to default to vi mode (`bindkey -v`), and mac already defaults to emacs mode.
# This is in *shared* to make it clear it is consistent, even though it is not changing mac.
bindkey -e

source ~/.config/zsh/os/bindkeys.sh

