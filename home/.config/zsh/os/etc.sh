# Coder workspace environment. The shared zsh config unconditionally sources
# this file; keep it present even when empty.

# No zellij auto-attach at work (IDE-embedded terminals would nest sessions);
# `zj` attaches manually. Flip this off to opt back in.
export ZJ_NO_AUTO=1
